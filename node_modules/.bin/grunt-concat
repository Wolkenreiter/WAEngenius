#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-concat/bin/grunt-concat" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-concat/bin/grunt-concat" "$@"
  ret=$?
fi
exit $ret
